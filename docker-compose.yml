version: '3.8'

services:
  purerepro-app:
    build: .
    ports:
      - "8000:8000"
    volumes:
      - .:/app
    env_file:
      - .env
    environment:
      - GEMINI_API_KEY=${GEMINI_API_KEY}
    restart: always

networks:
  default:
    name: purerepro-network
